<nav>
         <ul class="nl">
            {% for item in navigation %}
                <li class="fadeInNav">
                    <a href="{{item.url}}"  
                    {% if page.url == item.url %}class="current"{% endif %} >{{item.name}}</a>
                    
                </li>
            {% endfor %}
                <li class="fadeInNav"><a href="{{absolute_path_nl}}" class="current">NL</a>  <a href="{{absolute_path_en}}" class="">EN</a></li>
                <li class="fadeInNav"><a href="https://www.instagram.com/daanboertien.pianist/" target="_blank"><i class="fa-brands fa-instagram"></i></a>  <a href="https://www.facebook.com/daan.boertien/" target="_blank"><i class="fa-brands fa-square-facebook"></i></a></li>
            
        </ul>
        
</nav>


<div class="container-without-padding">
    <div class="hamburger-container">
        <button class="hamburger hamburger--spin" type="button">
            <span class="hamburger-box"></span>
              <span class="hamburger-inner hamburger-black"></span>
            
          </button>
    </div>
</div>

<div class="container navcontainer">
    <div class="navdesktop glass">
      <ul class="nl">
        {% for item in navigation %}
        <li class="fadeInTitle">
          <a
            href="{{item.url}}"
            class="navblack {% if page.url == item.url %}current {% endif %}"
            >{{item.name}}</a
          >
        </li>
        {% endfor %}
        <li class="fadeInTitle">
          <a href="{{absolute_path_nl}}" class="current">NL</a>
          <a href="{{absolute_path_en}}" class="navblack">EN</a>
        </li>
        <li class="fadeInTitle">
          <a
            href="https://www.instagram.com/daanboertien.pianist/"
            target="_blank"
            ><i class="fa-brands fa-instagram"></i
          ></a>
          <a href="https://www.facebook.com/daan.boertien/" target="_blank"
            ><i class="fa-brands fa-square-facebook"></i
          ></a>
        </li>
      </ul>
    </div>
  </div>